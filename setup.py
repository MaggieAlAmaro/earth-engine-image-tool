from setuptools import setup, find_packages

setup(
    name='ee-image-tools',
    version='0.0.1',
    description='',
    packages=find_packages(),
    install_requires=[
        'numpy',
        'PIL'
    ],
)